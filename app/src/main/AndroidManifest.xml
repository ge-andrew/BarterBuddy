<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BarterBuddy"
        tools:targetApi="31">
        <activity
            android:name=".activities.LoginPage"
            android:exported="false" />
        <activity
            android:name=".activities.CreateAccountPage"
            android:exported="false" />
        <activity
            android:name=".DemoUserPicker"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".utils.GetImageDialogBox"
            android:exported="false" />
        <activity
            android:name=".activities.AddNewItemPage"
            android:exported="false" />
        <activity
            android:name=".activities.UserItemDetailPage"
            android:exported="false" />
        <activity
            android:name=".activities.PublicItemDetailPage"
            android:exported="false" />
        <activity
            android:name=".activities.UserItemsPage"
            android:exported="false" />
        <activity
            android:name=".activities.ItemsAvailablePage"
            android:exported="false" />
    </application>

</manifest>